import React, { ReactElement } from "react";
import Card from "./Card";

interface Props {}

export default function InsurancePolicies({}: Props): ReactElement {
  return (
    <div>
      <Card />
    </div>
  );
}
